# POC-proga-6
## Run de Proyect
First install dependencies:
```sh
npm i electron
npm install child_process
npm install dotenv
```
Add your sudo password in `.env` file.

For run the proyect:
```sh
npm start
```
